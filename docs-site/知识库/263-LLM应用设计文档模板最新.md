# LLM 应用设计文档模板最新

> 知识库 70 有 138 行、知识库 167 和 246 有模板。这篇整合为最新精简版——一页纸设计文档。

---

## 一、精简设计模板

```markdown
# &#123;项目名&#125; 设计文档

## 基本信息
- 版本: v1.0 | 作者: &#123;姓名&#125; | 日期: &#123;日期&#125;

## 1. 目标
&#123;一句话目标&#125;
- 成功标准: &#123;可量化&#125;

## 2. 架构
&#123;Mermaid架构图&#125;
- 技术栈: LangGraph + &#123;其他&#125;

## 3. 数据
- 数据源: &#123;列表&#125;
- 分块: chunk_size=&#123;值&#125;
- 嵌入: &#123;模型&#125;
- 向量库: &#123;选择&#125;
- 安全: 脱敏+权限

## 4. 模型
- 主模型: &#123;GPT-4o&#125;
- 备用: &#123;mini&#125;
- 降级: &#123;链&#125;

## 5. 安全
- [ ] 输入验证
- [ ] PII脱敏
- [ ] 工具权限
- [ ] 高危审批

## 6. 运维
- SLO: &#123;可用性/延迟&#125;
- 监控: &#123;指标&#125;
- 告警: &#123;规则&#125;

## 7. 风险
| 风险 | 影响 | 缓解 |

## 8. 里程碑
| 阶段 | 日期 | 验收 |
```

---

## 二、评审检查器

```python
class DesignDocChecker:
    """设计文档评审检查器。"""

    MUST_HAVE = [
        "目标可量化", "有架构图", "技术栈选择",
        "数据安全方案", "降级策略", "SLO定义",
        "风险缓解", "里程碑",
    ]

    @classmethod
    def review(cls, doc: str) -> dict:
        """检查文档完整性。"""
        keywords = &#123;
            "目标可量化": ["成功标准", "量化", "指标"],
            "有架构图": ["mermaid", "graph", "架构"],
            "技术栈选择": ["LangGraph", "技术栈", "模型"],
            "数据安全方案": ["脱敏", "权限", "安全"],
            "降级策略": ["降级", "fallback", "备用"],
            "SLO定义": ["SLO", "可用性", "延迟"],
            "风险缓解": ["风险", "缓解", "措施"],
            "里程碑": ["里程碑", "MVP", "阶段"],
        &#125;
        missing = []
        for item in cls.MUST_HAVE:
            found = any(kw in doc for kw in keywords.get(item, [item]))
            if not found:
                missing.append(item)
        return &#123;
            "complete": len(missing) == 0,
            "missing": missing,
            "score": round(1 - len(missing) / len(cls.MUST_HAVE), 2),
        &#125;
```

---

## 三、检查清单

| 检查项 | 状态 |
|--------|------|
| 有精简模板 | ☐ |
| 有评审检查 | ☐ |
