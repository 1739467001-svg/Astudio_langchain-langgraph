# 技术术语表扩充最新

> 知识库 01 有 272 行。这篇扩充——新增 LangGraph、MCP、A2A、RAGAS 等最新术语。

---

## 一、核心术语

```python
class Glossary:
    """LLM应用开发术语表。"""

    TERMS = &#123;
        # 基础概念
        "LLM": "Large Language Model，大语言模型。如GPT-4o、Claude等。",
        "Token": "LLM处理的最小文本单元。英文~4字符=1token，中文~2字符=1token。",
        "Context Window": "上下文窗口，LLM一次能处理的最大Token数。GPT-4o为128K。",
        "Embedding": "嵌入向量。将文本转为数值向量，用于相似度计算。",
        "Vector Store": "向量数据库。存储和检索嵌入向量。如Milvus、Chroma。",
        "RAG": "Retrieval-Augmented Generation，检索增强生成。先检索再生成。",
        "Agent": "能自主决策、调用工具、多步推理的AI系统。",
        "Tool": "Agent可调用的外部功能。如搜索、计算、数据库查询。",

        # LangChain生态
        "LCEL": "LangChain Expression Language。用管道符|组合组件。",
        "Runnable": "LangChain v0.3的统一接口协议。支持invoke/stream/batch。",
        "Chain": "链。多个组件按顺序组合。v0.3中用LCEL替代旧Chain。",
        "PromptTemplate": "提示模板。参数化的提示文本。",
        "OutputParser": "输出解析器。将LLM输出转为结构化数据。",
        "Memory": "对话记忆。管理多轮对话的历史上下文。",

        # LangGraph
        "StateGraph": "状态图。LangGraph的核心概念，用图结构编排工作流。",
        "State": "状态。在图中流转的数据结构，用TypedDict定义。",
        "Node": "节点。图中的一个处理步骤。",
        "Edge": "边。连接节点的路径，决定执行顺序。",
        "Conditional Edge": "条件边。根据状态决定下一步去哪个节点。",
        "Reducer": "归约器。定义State字段的合并方式。如add_messages自动追加。",
        "Checkpointer": "检查点。保存每步状态，支持中断恢复和时间旅行。",
        "thread_id": "线程ID。用于隔离不同对话会话。",
        "ToolNode": "工具节点。批量执行Agent的工具调用。",
        "Command": "高级控制指令。resume/update/goto的组合。",
        "interrupt": "中断。暂停执行等待人工输入。",
        "RemoveMessage": "消息清理。从State中删除旧消息。",
        "Store": "长期存储。跨线程共享的用户级数据。",
        "astream_events": "事件级流式API。按事件类型（Token/工具/链）流式输出。",
        "Subgraph": "子图。可复用的子工作流，有独立State。",

        # RAG进阶
        "Chunking": "分块。将长文档切分为小块，便于检索。",
        "Hybrid Search": "混合检索。向量检索+关键词检索结合。",
        "Re-ranking": "重排序。用Cross-Encoder对检索结果精排。",
        "HyDE": "Hypothetical Document Embeddings。先让LLM生成假设答案再嵌入检索。",
        "RRF": "Reciprocal Rank Fusion。多路检索结果按排名倒数融合。",
        "RAGAS": "RAG评估框架。四大指标：忠实度/相关性/精确率/召回率。",
        "Self-RAG": "自我反思RAG。检索前后做相关性验证。",
        "Corrective RAG": "纠错RAG。检索质量低时转Web搜索补充。",
        "GraphRAG": "知识图谱RAG。图谱查关系+向量查语义。",

        # Agent模式
        "ReAct": "Reasoning+Acting。推理和行动交替的Agent模式。",
        "Plan-and-Execute": "先规划完整计划再逐步执行。",
        "Reflection": "自我反思。生成→评估→改进循环。",
        "CoT": "Chain of Thought。思维链。让LLM逐步推理。",
        "Few-Shot": "少样本学习。给LLM几个示例引导输出格式。",
        "Supervisor": "监督者模式。中心Agent调度其他工作Agent。",
        "MCP": "Model Context Protocol。Agent与工具通信的标准协议。",
        "A2A": "Agent-to-Agent Protocol。Agent间通信的协议。",
        "DST": "Dialog State Tracking。对话状态跟踪。",

        # 安全合规
        "OWASP LLM Top 10": "LLM应用10大安全风险。注入/泄露/过度代理等。",
        "PII": "Personally Identifiable Information。个人身份信息。",
        "Red Team": "红队测试。主动模拟攻击发现安全漏洞。",
        "Prompt Injection": "Prompt注入。恶意用户尝试覆盖系统指令。",

        # 评估运维
        "SLO": "Service Level Objective。服务等级目标。如99.9%可用。",
        "Error Budget": "错误预算。允许的故障时间。用完冻结新功能。",
        "TTFT": "Time To First Token。首Token延迟。用户感知的关键指标。",
        "TPS": "Tokens Per Second。Token生成速率。",
        "Semantic Cache": "语义缓存。用向量相似度判断是否命中缓存。",
        "Canary Release": "金丝雀发布。逐步扩大流量的发布策略。",
        "Feature Flag": "特性开关。运行时控制功能启停。",
    &#125;

    @classmethod
    def search(cls, term: str) -> str:
        """搜索术语定义。"""
        term_lower = term.lower().strip()
        for key, value in cls.TERMS.items():
            if key.lower() == term_lower:
                return value
        # 模糊匹配
        for key, value in cls.TERMS.items():
            if term_lower in key.lower():
                return value
        return f"未找到术语: &#123;term&#125;"

    @classmethod
    def by_category(cls, category: str) -> dict:
        """按分类获取术语。"""
        categories = &#123;
            "基础": ["LLM", "Token", "Context Window", "Embedding", "Vector Store", "RAG", "Agent", "Tool"],
            "LangChain": ["LCEL", "Runnable", "Chain", "PromptTemplate", "OutputParser", "Memory"],
            "LangGraph": ["StateGraph", "State", "Node", "Edge", "Conditional Edge", "Reducer", "Checkpointer", "thread_id", "ToolNode", "Command", "interrupt", "RemoveMessage", "Store", "astream_events", "Subgraph"],
            "RAG进阶": ["Chunking", "Hybrid Search", "Re-ranking", "HyDE", "RRF", "RAGAS", "Self-RAG", "Corrective RAG", "GraphRAG"],
            "Agent模式": ["ReAct", "Plan-and-Execute", "Reflection", "CoT", "Few-Shot", "Supervisor", "MCP", "A2A", "DST"],
            "安全合规": ["OWASP LLM Top 10", "PII", "Red Team", "Prompt Injection"],
            "评估运维": ["SLO", "Error Budget", "TTFT", "TPS", "Semantic Cache", "Canary Release", "Feature Flag"],
        &#125;
        cat_terms = categories.get(category, [])
        return &#123;term: cls.TERMS.get(term, "") for term in cat_terms&#125;
```

---

## 二、检查清单

| 检查项 | 状态 |
|--------|------|
| 术语覆盖7大分类 | ☐ |
| 可搜索查询 | ☐ |
