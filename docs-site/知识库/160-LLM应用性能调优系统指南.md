# LLM 应用性能调优系统指南

> LLM 应用慢的原因可能不在 LLM——向量检索慢、重排序耗时、对话历史太长、缓存没命中。这份指南从全链路视角定位瓶颈，给出系统化的调优策略。

---

## 一、全链路性能分析

```mermaid
graph LR
    subgraph 链路 &#123;"LLM请求全链路"&#125;
        A["接收请求<br/>~1ms"] --> B["缓存检查<br/>~5ms"]
        B --> C["意图分类<br/>~200ms"]
        C --> D["向量检索<br/>~20ms"]
        D --> E["重排序<br/>~100ms"]
        E --> F["上下文组装<br/>~5ms"]
        F --> G["LLM调用<br/>~2000ms"]
        G --> H["输出处理<br/>~10ms"]
        H --> I["返回响应<br/>~1ms"]
    end

    style G fill:#FFCDD2,stroke:#C62828,stroke-width:3px
    style C fill:#FFF9C4
    style E fill:#FFF9C4
```

```python
import time
from functools import wraps
from dataclasses import dataclass, field
from collections import defaultdict

@dataclass
class TraceSpan:
    """性能追踪Span。"""
    name: str
    start: float
    end: float = 0
    duration_ms: float = 0

@dataclass
class PerformanceTrace:
    """全链路性能追踪。"""
    spans: list[TraceSpan] = field(default_factory=list)
    _current: TraceSpan | None = None

    def start_span(self, name: str):
        if self._current:
            self._current.end = time.time()
            self._current.duration_ms = round((self._current.end - self._current.start) * 1000, 2)
            self.spans.append(self._current)
        self._current = TraceSpan(name=name, start=time.time())

    def end_trace(self) -> dict:
        if self._current:
            self._current.end = time.time()
            self._current.duration_ms = round((self._current.end - self._current.start) * 1000, 2)
            self.spans.append(self._current)
            self._current = None

        total = sum(s.duration_ms for s in self.spans)
        return &#123;
            "total_ms": round(total, 2),
            "spans": [
                &#123;"name": s.name, "ms": s.duration_ms, "pct": round(s.duration_ms / total * 100, 1)&#125;
                for s in self.spans
            ],
            "bottleneck": max(self.spans, key=lambda s: s.duration_ms).name if self.spans else "none",
        &#125;

def trace_performance(func):
    """性能追踪装饰器。"""
    @wraps(func)
    async def wrapper(*args, **kwargs):
        trace = kwargs.get("trace") or PerformanceTrace()
        trace.start_span(func.__name__)
        result = await func(*args, **kwargs)
        trace.end_trace()
        return result
    return wrapper
```

---

## 二、各环节调优策略

```mermaid
graph TB
    subgraph 调优 &#123;"各环节调优策略"&#125;
        S1["LLM调用<br/>最大瓶颈<br/>→模型路由+缓存"]
        S2["向量检索<br/>→HNSW调优+减小k"]
        S3["重排序<br/>→减小recall_k或跳过"]
        S4["对话历史<br/>→摘要压缩"]
        S5["意图分类<br/>→用小模型"]
        S6["缓存命中<br/>→语义缓存"]
    end

    style S1 fill:#FFCDD2,stroke:#C62828,stroke-width:3px
    style S6 fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
```

### 2.1 LLM 调用优化（最大瓶颈）

```python
class LLMCallOptimizer:
    """LLM调用优化器。"""

    STRATEGIES = &#123;
        "model_routing": &#123;
            "desc": "简单查询用小模型",
            "saving": "60-80%延迟",
            "priority": "★★★",
        &#125;,
        "semantic_cache": &#123;
            "desc": "相似查询直接返回缓存",
            "saving": "100%延迟(命中时)",
            "priority": "★★★",
        &#125;,
        "streaming": &#123;
            "desc": "流式输出，首Token<500ms",
            "saving": "感知延迟降低80%",
            "priority": "★★★",
        &#125;,
        "batch_api": &#123;
            "desc": "非实时场景用批量API",
            "saving": "50%成本",
            "priority": "★★☆",
        &#125;,
        "context_compression": &#123;
            "desc": "压缩检索上下文",
            "saving": "30-50%Token→延迟降",
            "priority": "★★☆",
        &#125;,
        "max_tokens_limit": &#123;
            "desc": "限制输出长度",
            "saving": "防止过长输出",
            "priority": "★★☆",
        &#125;,
    &#125;
```

### 2.2 向量检索优化

```python
class VectorSearchOptimizer:
    """向量检索优化。"""

    STRATEGIES = &#123;
        "hnsw_ef_search": &#123;
            "desc": "降低ef_search(64→32)",
            "saving": "50%查询延迟",
            "tradeoff": "召回率略降",
        &#125;,
        "reduce_k": &#123;
            "desc": "减少Top-K(10→5)",
            "saving": "减半检索+后处理时间",
            "tradeoff": "可能遗漏文档",
        &#125;,
        "pre_filter": &#123;
            "desc": "先用metadata过滤再向量搜索",
            "saving": "减少搜索范围",
            "tradeoff": "需要好的metadata",
        &#125;,
        "approximate_search": &#123;
            "desc": "用ANN近似搜索替代精确搜索",
            "saving": "10-100x加速",
            "tradeoff": "精度略降",
        &#125;,
    &#125;
```

### 2.3 重排序优化

```python
class RerankOptimizer:
    """重排序优化——通常是第二大瓶颈。"""

    STRATEGIES = &#123;
        "reduce_recall_k": &#123;
            "desc": "减少重排序候选数(20→10)",
            "saving": "50%重排序时间",
        &#125;,
        "skip_for_simple": &#123;
            "desc": "简单查询跳过重排序",
            "saving": "100%重排序时间(跳过时)",
        &#125;,
        "use_faster_model": &#123;
            "desc": "用轻量重排序模型",
            "saving": "30-50%延迟",
        &#125;,
        "async_rerank": &#123;
            "desc": "先返回初步结果，后台重排序后更新",
            "saving": "用户感知0延迟",
        &#125;,
    &#125;
```

---

## 三、性能基准测试

```python
import asyncio
import statistics

class PerformanceBenchmark:
    """性能基准测试器。"""

    @staticmethod
    async def benchmark(
        func,
        inputs: list,
        concurrency: int = 1,
    ) -> dict:
        """基准测试。

        Args:
            func: 异步函数
            inputs: 输入列表
            concurrency: 并发数
        """
        latencies = []
        errors = 0

        # 并发执行
        semaphore = asyncio.Semaphore(concurrency)

        async def run_one(input_data):
            nonlocal errors
            async with semaphore:
                start = time.time()
                try:
                    await func(input_data)
                    latencies.append((time.time() - start) * 1000)
                except Exception:
                    errors += 1

        tasks = [run_one(inp) for inp in inputs]
        await asyncio.gather(*tasks)

        if not latencies:
            return &#123;"error": "无成功请求"&#125;

        latencies.sort()
        n = len(latencies)

        return &#123;
            "total_requests": len(inputs),
            "successful": n,
            "failed": errors,
            "avg_latency_ms": round(statistics.mean(latencies), 2),
            "p50_ms": round(latencies[n // 2], 2),
            "p95_ms": round(latencies[int(n * 0.95)], 2),
            "p99_ms": round(latencies[int(n * 0.99)], 2),
            "qps": round(n / (sum(latencies) / 1000 / concurrency), 2),
        &#125;
```

---

## 四、调优决策树

```mermaid
graph TB
    Q1["延迟>3s？"] -->|是| Q2["瓶颈在哪？"]
    Q2 -->|LLM调用| Q3&#123;"命中率>30%?"&#125;
    Q3 -->|否| C1["+语义缓存"]
    Q3 -->|是| Q4&#123;"简单查询占比高?"&#125;
    Q4 -->|是| C2["+模型路由"]
    Q4 -->|否| C3["+上下文压缩"]
    Q2 -->|向量检索| C4["调HNSW参数"]
    Q2 -->|重排序| C5["减小recall_k"]
    Q2 -->|对话历史| C6["+历史压缩"]
    Q1 -->|否| OK["性能可接受"]

    style C1 fill:#C8E6C9
    style C2 fill:#C8E6C9
```

---

## 五、最佳实践

| 优化项 | 延迟降低 | 实施难度 | 优先级 |
|--------|----------|----------|--------|
| 语义缓存 | 100%(命中时) | 中 | ★★★ |
| 模型路由 | 60-80% | 中 | ★★★ |
| 流式输出 | 80%感知延迟 | 低 | ★★★ |
| 上下文压缩 | 30-50% | 中 | ★★☆ |
| 减小recall_k | 50%重排序 | 低 | ★★☆ |
| HNSW调优 | 50%检索 | 低 | ★★☆ |
| 对话历史压缩 | 50-70% | 中 | ★★☆ |

---

## 六、检查清单

| 检查项 | 状态 |
|--------|------|
| 有全链路追踪 | ☐ |
| 识别了瓶颈环节 | ☐ |
| 启用了语义缓存 | ☐ |
| 启用了模型路由 | ☐ |
| 启用了流式输出 | ☐ |
| 做了基准测试 | ☐ |
