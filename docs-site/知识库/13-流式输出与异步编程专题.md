# 流式输出与异步编程专题

> 流式输出让用户逐字看到回复，异步让应用同时处理多请求。这是生产级 LLM 应用的必备技能。

---

## 一、为什么需要流式和异步

```mermaid
graph TB
    subgraph 同步阻塞 ["同步阻塞（用户体验差）"]
        S1["用户提问"] --> S2["等待LLM<br/>(3-10秒)"]
        S2 --> S3["一次性返回结果"]
        S3 --> S4["用户看到回答"]

        Note1["问题: 用户盯着空白页面3-10秒<br/>以为卡住了"]
    end

    subgraph 流式输出 ["流式输出（用户体验好）"]
        F1["用户提问"] --> F2["LLM开始生成"]
        F2 --> F3["第1个token<br/>(0.3秒)"]
        F3 --> F4["第2个token"]
        F4 --> F5["第3个token..."]
        F5 --> F6["完成"]

        Note2["优势: 首字时间0.3秒<br/>用户立即看到输出"]
    end

    style 同步阻塞 fill:#FFCDD2
    style 流式输出 fill:#C8E6C9
```

```mermaid
graph TB
    subgraph 单请求同步 ["单请求同步"]
        R1["请求1"] --> W1["等待..."]
        W1 --> D1["完成"]
        D1 --> R2["请求2"]
        R2 --> W2["等待..."]
        W2 --> D2["完成"]
        Note1["两个请求总耗时: 6秒"]
    end

    subgraph 多请求异步 ["多请求异步"]
        A1["请求1"] & A2["请求2"]
        A1 --> AW1["await"]
        A2 --> AW2["await"]
        AW1 --> AD1["完成"]
        AW2 --> AD2["完成"]
        Note2["两个请求总耗时: 3秒(并发)"]
    end

    style 单请求同步 fill:#FFCDD2
    style 多请求异步 fill:#C8E6C9
```

## 二、流式输出

### 2.1 基础流式

```python
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser

llm = ChatOpenAI(model="gpt-4o-mini")
prompt = ChatPromptTemplate.from_template("用200字解释&#123;concept&#125;")
chain = prompt | llm | StrOutputParser()

# 流式输出
print("回答: ", end="", flush=True)
for chunk in chain.stream(&#123;"concept": "量子计算"&#125;):
    print(chunk, end="", flush=True)
print()  # 换行
```

### 2.2 流式输出的数据流

```mermaid
sequenceDiagram
    participant U as 用户代码
    participant C as Chain
    participant L as LLM API
    participant O as 输出

    U->>C: chain.stream(input)
    C->>L: 发送API请求(stream=true)
    
    loop 逐Token返回
        L-->>C: chunk1 ("量")
        C-->>U: yield "量"
        U->>O: print("量")
        
        L-->>C: chunk2 ("子")
        C-->>U: yield "子"
        U->>O: print("子")
    end
    
    L-->>C: [DONE]
    C-->>U: 结束迭代
```

### 2.3 异步流式（Web应用用）

```python
import asyncio

async def async_stream():
    chain = prompt | llm | StrOutputParser()
    
    async for chunk in chain.astream(&#123;"concept": "量子计算"&#125;):
        print(chunk, end="", flush=True)
    
    print()

# 运行
asyncio.run(async_stream())
```

### 2.4 LangGraph 流式

```python
# LangGraph 的流式输出
from langgraph.graph import StateGraph, START, END

# 编译图
app = graph.compile()

# 方式1：节点级别流式（每执行完一个节点输出一次）
for event in app.stream(input_data, config=config):
    print(f"节点输出: &#123;event&#125;")

# 方式2：Token级别流式（LLM逐Token输出）
async for event in app.astream_events(
    input_data,
    config=config,
    version="v2"
):
    if event["event"] == "on_chat_model_stream":
        # 只输出LLM生成的token
        chunk = event["data"]["chunk"]
        print(chunk.content, end="", flush=True)
```

### 2.5 流式 vs 非流式对比

```mermaid
graph TB
    subgraph invoke ["invoke（等待完成）"]
        I1["调用"] --> I2["等待...<br/>(3秒)"] --> I3["返回完整结果"]
    end

    subgraph stream ["stream（逐字返回）"]
        S1["调用"] --> S2["0.3秒返回第1块"] --> S3["0.5秒返回第2块"]
        S3 --> S4["0.7秒返回第3块"] --> S5["3秒完成"]
    end

    style I2 fill:#FFCDD2
    style S2 fill:#C8E6C9
```

## 三、异步编程

### 3.1 同步 vs 异步

```python
import time
import asyncio

# --- 同步（串行）---
def sync_example():
    start = time.time()
    
    result1 = llm.invoke("问题1")  # 等待3秒
    result2 = llm.invoke("问题2")  # 再等3秒
    result3 = llm.invoke("问题3")  # 再等3秒
    
    print(f"总耗时: &#123;time.time()-start:.1f&#125;s")  # 约9秒

# --- 异步（并发）---
async def async_example():
    start = time.time()
    
    # 三个请求同时发起
    tasks = [
        llm.ainvoke("问题1"),
        llm.ainvoke("问题2"),
        llm.ainvoke("问题3"),
    ]
    results = await asyncio.gather(*tasks)
    
    print(f"总耗时: &#123;time.time()-start:.1f&#125;s")  # 约3秒（并发）

asyncio.run(async_example())
```

### 3.2 异步执行流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant E as asyncio.gather
    participant L as LLM API

    U->>E: gather(task1, task2, task3)
    
    par 并发执行
        E->>L: 请求1 (3秒)
        Note over L: 处理中...
    and
        E->>L: 请求2 (3秒)
        Note over L: 处理中...
    and
        E->>L: 请求3 (3秒)
        Note over L: 处理中...
    end
    
    L-->>E: 结果1
    L-->>E: 结果2
    L-->>E: 结果3
    E-->>U: [结果1, 结果2, 结果3]
    Note over U: 总耗时约3秒（取最慢的）
```

### 3.3 异步 Chain

```python
# 异步调用 Chain
chain = prompt | llm | StrOutputParser()

# 单条异步
result = await chain.ainvoke(&#123;"topic": "AI"&#125;)

# 异步流式
async for chunk in chain.astream(&#123;"topic": "AI"&#125;):
    print(chunk, end="")

# 异步批量
results = await chain.abatch([
    &#123;"topic": "AI"&#125;,
    &#123;"topic": "区块链"&#125;,
    &#123;"topic": "量子计算"&#125;,
])
```

### 3.4 异步批处理 vs 同步批处理

```mermaid
graph TB
    subgraph batch ["batch（同步批量）"]
        B1["3条输入"] --> B2["内部自动并发"]
        B2 --> B3["等待全部完成"]
        B3 --> B4["返回3条结果"]
    end

    subgraph abatch ["abatch（异步批量）"]
        A1["3条输入"] --> A2["内部自动并发"]
        A2 --> A3["await全部完成"]
        A3 --> A4["返回3条结果"]
    end

    subgraph gather ["gather（手动异步）"]
        G1["手动创建3个任务"] --> G2["asyncio.gather"]
        G2 --> G3["并发执行"]
        G3 --> G4["返回3条结果"]
    end

    style batch fill:#E3F2FD
    style abatch fill:#C8E6C9
    style gather fill:#FFF3E0
```

## 四、FastAPI 中的流式和异步

```python
from fastapi import FastAPI
from fastapi.responses import StreamingResponse
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser
from pydantic import BaseModel

app = FastAPI()
llm = ChatOpenAI(model="gpt-4o-mini")

chain = (
    ChatPromptTemplate.from_template("回答问题：&#123;question&#125;")
    | llm
    | StrOutputParser()
)

class QuestionRequest(BaseModel):
    question: str

# 同步端点
@app.post("/ask")
async def ask(request: QuestionRequest):
    result = await chain.ainvoke(&#123;"question": request.question&#125;)
    return &#123;"answer": result&#125;

# 流式端点（逐字返回到前端）
@app.post("/ask/stream")
async def ask_stream(request: QuestionRequest):
    async def generate():
        async for chunk in chain.astream(&#123;"question": request.question&#125;):
            yield f"data: &#123;chunk&#125;\n\n"
    
    return StreamingResponse(generate(), media_type="text/event-stream")

# 运行: uvicorn server:app --reload
```

### 请求流程

```mermaid
sequenceDiagram
    participant C as 客户端
    participant F as FastAPI
    participant L as LLM

    C->>F: POST /ask/stream &#123;"question": "什么是AI?"&#125;
    F->>L: astream 请求
    
    loop 逐Token
        L-->>F: chunk1 ("A")
        F-->>C: data: A\n\n
        L-->>F: chunk2 ("I")
        F-->>C: data: I\n\n
    end
    
    L-->>F: [DONE]
    F-->>C: 关闭连接
```

## 五、异步编程要点

```mermaid
graph TB
    subgraph 异步要点
        R1["async def 定义异步函数"]
        R2["await 调用异步函数"]
        R3["asyncio.gather 并发多个"]
        R4["async for 异步迭代"]
        R5["asyncio.run() 启动事件循环"]
    end

    subgraph 注意事项
        W1["同步和异步不要混用"]
        W2["异步函数中不能有阻塞操作"]
        W3["FastAPI/Flask中用异步提升并发"]
    end

    style R1 fill:#C8E6C9
    style W1 fill:#FFCDD2
```

### 常见陷阱

```python
# ❌ 错误：在异步函数中使用同步调用
async def bad_example():
    # 这会阻塞事件循环！
    result = llm.invoke("question")  # 同步调用阻塞了整个事件循环
    return result

# ✅ 正确：使用异步调用
async def good_example():
    result = await llm.ainvoke("question")  # 非阻塞
    return result

# ❌ 错误：同步函数中调用异步函数
def sync_calling_async():
    # 需要用 asyncio.run() 包装
    result = chain.ainvoke(&#123;"input": "test"&#125;)  # 报错：返回coroutine
    return result

# ✅ 正确
def sync_calling_async_fixed():
    result = asyncio.run(chain.ainvoke(&#123;"input": "test"&#125;))
    return result

# ✅ 最佳：全程异步
async def best_example():
    tasks = [
        chain.ainvoke(&#123;"input": "问题1"&#125;),
        chain.ainvoke(&#123;"input": "问题2"&#125;),
    ]
    results = await asyncio.gather(*tasks)
    return results
```

## 六、性能对比实测

```python
import time
import asyncio
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(model="gpt-4o-mini")

# 测试同步
def test_sync():
    start = time.time()
    for _ in range(5):
        llm.invoke("说一个词")
    return time.time() - start

# 测试批量
def test_batch():
    start = time.time()
    llm.batch(["说一个词"] * 5)
    return time.time() - start

# 测试异步
async def test_async():
    start = time.time()
    tasks = [llm.ainvoke("说一个词") for _ in range(5)]
    await asyncio.gather(*tasks)
    return time.time() - start

# 运行对比
print(f"同步串行: &#123;test_sync():.2f&#125;s")     # ~15秒 (3s × 5)
print(f"批量并发: &#123;test_batch():.2f&#125;s&#125;")    # ~3秒
print(f"异步并发: &#123;asyncio.run(test_async()):.2f&#125;s&#125;")  # ~3秒
```
