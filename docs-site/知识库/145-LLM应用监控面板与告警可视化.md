# LLM 应用监控面板与告警可视化

> 有指标采集器（知识库 123）和告警规则（知识库 89），但如果没有可视化面板，运维就是盲人摸象。这份指南覆盖 LLM 应用的监控仪表盘设计、Grafana 集成和告警可视化。

---

## 一、监控面板全景

```mermaid
graph TB
    subgraph 面板 &#123;"LLM应用监控面板体系"&#125;
        D1["实时概览<br/>QPS/延迟/错误率/成本"]
        D2["模型分析<br/>各模型调用量/成本/延迟"]
        D3["用户行为<br/>活跃用户/会话/满意度"]
        D4["资源监控<br/>向量库/缓存/队列"]
        D5["告警视图<br/>活跃告警/历史/趋势"]
    end

    style 面板 fill:#E3F2FD
```

---

## 二、指标导出格式

```python
class PrometheusExporter:
    """将LLM应用指标导出为Prometheus格式。

    可被Grafana直接消费。
    """

    def __init__(self):
        self.metrics: dict[str, list] = &#123;&#125;

    def record(self, name: str, value: float, **labels):
        """记录指标。"""
        if name not in self.metrics:
            self.metrics[name] = []
        label_str = ",".join(f'&#123;k&#125;="&#123;v&#125;"' for k, v in sorted(labels.items()))
        self.metrics[name].append((value, label_str))

    def export(self) -> str:
        """导出为Prometheus文本格式。"""
        lines = []
        for name, records in self.metrics.items():
            for value, labels in records:
                if labels:
                    lines.append(f"&#123;name&#125;&#123;&#123;&#123;labels&#125;&#125;&#125; &#123;value&#125;")
                else:
                    lines.append(f"&#123;name&#125; &#123;value&#125;")
        return "\n".join(lines)

# 使用示例
exporter = PrometheusExporter()
exporter.record("llm_request_total", 1, model="gpt-4o", status="success")
exporter.record("llm_latency_seconds", 1.5, model="gpt-4o")
exporter.record("llm_tokens_total", 350, model="gpt-4o", type="input")
exporter.record("llm_cost_usd", 0.002, model="gpt-4o")
# 输出:
# llm_request_total&#123;model="gpt-4o",status="success"&#125; 1
# llm_latency_seconds&#123;model="gpt-4o"&#125; 1.5
```

---

## 三、FastAPI 指标端点

```python
from fastapi import FastAPI, Response
import time
from collections import defaultdict

app = FastAPI()

class MetricsCollector:
    """简化版指标采集器。"""
    def __init__(self):
        self.counters = defaultdict(float)
        self.histograms = defaultdict(list)

    def inc(self, name: str, value: float = 1, **labels):
        key = f"&#123;name&#125;&#123;&#123;&#123;','.join(f'&#123;k&#125;=&#123;v&#125;' for k,v in sorted(labels.items()))&#125;&#125;&#125;"
        self.counters[key] += value

    def observe(self, name: str, value: float, **labels):
        self.histograms[name].append(value)
        self.inc(name + "_count", 1, **labels)
        self.inc(name + "_sum", value, **labels)

    def export_prometheus(self) -> str:
        lines = []
        for key, val in self.counters.items():
            lines.append(f"&#123;key&#125; &#123;val&#125;")
        for name, values in self.histograms.items():
            if values:
                lines.append(f"&#123;name&#125;_count &#123;len(values)&#125;")
                lines.append(f"&#123;name&#125;_sum &#123;sum(values)&#125;")
                lines.append(f"&#123;name&#125;_avg &#123;sum(values)/len(values):.4f&#125;")
        return "\n".join(lines)

metrics = MetricsCollector()

@app.get("/metrics")
async def prometheus_metrics():
    """Prometheus指标端点。"""
    return Response(
        content=metrics.export_prometheus(),
        media_type="text/plain",
    )

@app.post("/chat")
async def chat(request: dict):
    """带指标采集的聊天端点。"""
    start = time.time()
    # 调用LLM...
    latency = time.time() - start

    metrics.inc("llm_request_total", 1, model="gpt-4o", status="success")
    metrics.observe("llm_latency_seconds", latency, model="gpt-4o")
    metrics.inc("llm_tokens_total", 350, model="gpt-4o", type="input")

    return &#123;"response": "回答"&#125;
```

---

## 四、Grafana 仪表盘配置

```mermaid
graph TB
    subgraph 架构 &#123;"Grafana监控架构"&#125;
        APP["LLM应用<br/>FastAPI"] --> METRICS["GET /metrics<br/>Prometheus格式"]
        METRICS --> PROM["Prometheus<br/>采集+存储"]
        PROM --> GRAFANA["Grafana<br/>可视化面板"]
        GRAFANA --> DASH["仪表盘"]
        GRAFANA --> ALERT["告警规则"]
        ALERT --> NOTIFY["通知渠道<br/>邮件/Slack/钉钉"]
    end

    style PROM fill:#E3F2FD
    style GRAFANA fill:#FFF9C4,stroke:#F9A825,stroke-width:3px
    style ALERT fill:#FFCDD2
```

```json
// grafana-dashboard.json — Grafana仪表盘配置（核心面板）
&#123;
  "dashboard": &#123;
    "title": "LLM应用监控",
    "panels": [
      &#123;
        "title": "请求QPS",
        "type": "stat",
        "targets": [&#123;"expr": "rate(llm_request_total[5m])"&#125;]
      &#125;,
      &#123;
        "title": "P95延迟",
        "type": "stat",
        "targets": [&#123;"expr": "histogram_quantile(0.95, rate(llm_latency_seconds_bucket[5m]))"&#125;]
      &#125;,
      &#123;
        "title": "错误率",
        "type": "stat",
        "targets": [&#123;"expr": "rate(llm_request_total&#123;status=\"error\"&#125;[5m]) / rate(llm_request_total[5m])"&#125;]
      &#125;,
      &#123;
        "title": "Token消耗趋势",
        "type": "graph",
        "targets": [&#123;"expr": "rate(llm_tokens_total[1h])"&#125;]
      &#125;,
      &#123;
        "title": "各模型成本",
        "type": "pie",
        "targets": [&#123;"expr": "sum(llm_cost_usd) by (model)"&#125;]
      &#125;,
      &#123;
        "title": "缓存命中率",
        "type": "gauge",
        "targets": [&#123;"expr": "cache_hit_total / (cache_hit_total + cache_miss_total) * 100"&#125;]
      &#125;
    ]
  &#125;
&#125;
```

---

## 五、告警可视化

```python
class AlertDashboard:
    """告警可视化数据生成器。"""

    def __init__(self):
        self.alerts: list[dict] = []

    def add_alert(
        self,
        name: str,
        severity: str,
        message: str,
        value: float,
        threshold: float,
    ):
        """添加告警。"""
        self.alerts.append(&#123;
            "name": name,
            "severity": severity,
            "message": message,
            "value": value,
            "threshold": threshold,
            "timestamp": time.time(),
            "status": "firing",
        &#125;)

    def resolve_alert(self, name: str):
        """解除告警。"""
        for alert in self.alerts:
            if alert["name"] == name and alert["status"] == "firing":
                alert["status"] = "resolved"
                alert["resolved_at"] = time.time()

    def active_alerts(self) -> list[dict]:
        """活跃告警。"""
        return [a for a in self.alerts if a["status"] == "firing"]

    def summary(self) -> dict:
        """告警摘要。"""
        active = self.active_alerts()
        by_severity = &#123;&#125;
        for a in active:
            by_severity[a["severity"]] = by_severity.get(a["severity"], 0) + 1

        return &#123;
            "total_active": len(active),
            "by_severity": by_severity,
            "critical": by_severity.get("critical", 0),
            "high": by_severity.get("high", 0),
            "warning": by_severity.get("warning", 0),
        &#125;
```

---

## 六、自定义面板生成

```python
class DashboardGenerator:
    """动态生成监控面板数据。"""

    @staticmethod
    def generate_overview(metrics: dict) -> dict:
        """生成实时概览面板。"""
        return &#123;
            "title": "LLM应用实时概览",
            "updated_at": time.time(),
            "panels": [
                &#123;
                    "name": "当前QPS",
                    "value": metrics.get("current_qps", 0),
                    "unit": "req/s",
                    "threshold": &#123;"warning": 50, "critical": 100&#125;,
                &#125;,
                &#123;
                    "name": "P95延迟",
                    "value": metrics.get("p95_latency", 0),
                    "unit": "ms",
                    "threshold": &#123;"warning": 2000, "critical": 5000&#125;,
                &#125;,
                &#123;
                    "name": "错误率",
                    "value": metrics.get("error_rate", 0),
                    "unit": "%",
                    "threshold": &#123;"warning": 5, "critical": 10&#125;,
                &#125;,
                &#123;
                    "name": "今日成本",
                    "value": metrics.get("daily_cost", 0),
                    "unit": "$",
                    "threshold": &#123;"warning": 50, "critical": 100&#125;,
                &#125;,
                &#123;
                    "name": "缓存命中率",
                    "value": metrics.get("cache_hit_rate", 0),
                    "unit": "%",
                    "threshold": &#123;"warning": 20, "critical": 10&#125;,
                &#125;,
            ],
        &#125;
```

---

## 七、通知渠道集成

```python
class NotificationSender:
    """多渠道告警通知。"""

    @staticmethod
    async def send_slack(webhook_url: str, alert: dict):
        """发送Slack通知。"""
        import httpx
        message = &#123;
            "text": f"🚨 &#123;alert['severity'].upper()&#125;: &#123;alert['name']&#125;",
            "attachments": [&#123;
                "color": "danger" if alert["severity"] == "critical" else "warning",
                "fields": [
                    &#123;"title": "告警", "value": alert["message"], "short": False&#125;,
                    &#123;"title": "当前值", "value": str(alert["value"]), "short": True&#125;,
                    &#123;"title": "阈值", "value": str(alert["threshold"]), "short": True&#125;,
                ],
            &#125;],
        &#125;
        async with httpx.AsyncClient() as client:
            await client.post(webhook_url, json=message)

    @staticmethod
    async def send_webhook(url: str, alert: dict):
        """发送Webhook通知。"""
        import httpx
        async with httpx.AsyncClient() as client:
            await client.post(url, json=alert)
```

---

## 八、最佳实践

| 实践 | 说明 | 优先级 |
|------|------|--------|
| 暴露/metrics端点 | Prometheus标准格式 | ★★★ |
| Grafana做可视化 | 行业标准工具 | ★★☆ |
| 面板分5个维度 | 概览/模型/用户/资源/告警 | ★★★ |
| 告警分3级 | warning/high/critical | ★★☆ |
| 多渠道通知 | Slack/邮件/钉钉 | ★★☆ |
| 延迟看P95不看平均 | 平均被异常值拉偏 | ★★★ |

---

## 九、检查清单

| 检查项 | 状态 |
|--------|------|
| 暴露了/metrics端点 | ☐ |
| 配置了Grafana面板 | ☐ |
| 有实时概览面板 | ☐ |
| 有模型分析面板 | ☐ |
| 有告警可视化 | ☐ |
| 有多渠道通知 | ☐ |
