# 学习课程 07：RAG 检索增强生成最新

> 学习课程 07 有 355 行。这篇基于 v0.3 更新——LCEL RAG 管道和 LangGraph RAG 图。

---

## 一、RAG 全流程

```mermaid
graph LR
    subgraph 离线 &#123;"离线建库"&#125;
        D["文档"] --> SPLIT["分块"]
        SPLIT --> EMBED["嵌入"]
        EMBED --> STORE["存入向量库"]
    end

    subgraph 在线 &#123;"在线检索"&#125;
        Q["查询"] --> RET["向量检索Top-K"]
        RET --> GEN["LLM生成"]
        GEN --> A["回答"]
    end

    style 离线 fill:#E3F2FD
    style 在线 fill:#FFF3E0
```

---

## 二、离线建库

```python
from langchain_text_splitters import RecursiveCharacterTextSplitter
from langchain_openai import OpenAIEmbeddings
from langchain_community.vectorstores import Chroma

# 1. 分块
splitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=50)
chunks = splitter.split_text(document_text)

# 2. 嵌入+入库
embeddings = OpenAIEmbeddings()
vectorstore = Chroma.from_texts(chunks, embeddings, persist_directory="./chroma_db")
```

---

## 三、在线检索

```python
# LCEL管道方式
from langchain_core.runnables import RunnablePassthrough

retriever = vectorstore.as_retriever(search_kwargs=&#123;"k": 3&#125;)

rag_chain = (
    &#123;"context": retriever, "question": RunnablePassthrough()&#125;
    | ChatPromptTemplate.from_template("基于以下信息回答:\n&#123;context&#125;\n\n问题: &#123;question&#125;")
    | ChatOpenAI(model="gpt-4o-mini")
    | StrOutputParser()
)

result = rag_chain.invoke("什么是向量数据库?")
```

---

## 四、最佳实践

| 实践 | 说明 | 优先级 |
|------|------|--------|
| chunk_size=500 | 平衡精度和上下文 | ★★★ |
| k=3-5 | 不贪多 | ★★★ |
| 用LCEL管道 | 简洁 | ★★★ |
| 有引用来源 | 可追溯 | ★★☆ |

---

## 五、检查清单

| 检查项 | 状态 |
|--------|------|
| 能建向量库 | ☐ |
| 能构建RAG管道 | ☐ |
