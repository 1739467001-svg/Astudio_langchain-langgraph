# 用户画像与个性化最新

> 知识库 60 仅 161 行。这篇讲透——用户画像怎么构建、如何从对话中自动学习、如何注入 Prompt 实现个性化。

---

## 一、用户画像架构

```mermaid
graph TB
    subgraph 画像 &#123;"用户画像三层"&#125;
        L1["基础层<br/>ID/姓名/语言/地区"]
        L2["偏好层<br/>回答风格/深度/格式"]
        L3["行为层<br/>历史查询/评分/修正"]
    end

    subgraph 流程 &#123;"画像使用流程"&#125;
        BUILD["从对话学习"] --> STORE["持久化存储"]
        STORE --> INJECT["注入系统提示"]
        INJECT --> PERSONALIZED["个性化回答"]
    end

    style 画像 fill:#E3F2FD
    style INJECT fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
```

---

## 二、实现

```python
from dataclasses import dataclass, field
from datetime import datetime
from langchain_core.language_models import BaseChatModel
from langchain_core.messages import HumanMessage
import json, re

@dataclass
class UserProfile:
    """用户画像。"""
    user_id: str
    name: str = ""
    language: str = "zh"
    region: str = ""
    preferences: dict = field(default_factory=dict)  # &#123;category: value&#125;
    history: list[dict] = field(default_factory=list)
    ratings: dict = field(default_factory=dict)
    skills: list[str] = field(default_factory=list)  # 用户技能水平

    def add_preference(self, key: str, value: str):
        self.preferences[key] = &#123;"value": value, "updated_at": datetime.now().isoformat()&#125;

    def get_profile_text(self) -> str:
        """生成画像文本注入Prompt。"""
        lines = []
        if self.name:
            lines.append(f"用户名: &#123;self.name&#125;")
        if self.language:
            lines.append(f"语言: &#123;self.language&#125;")
        if self.preferences:
            for k, v in self.preferences.items():
                lines.append(f"&#123;k&#125;: &#123;v['value']&#125;")
        if self.skills:
            lines.append(f"技能水平: &#123;', '.join(self.skills)&#125;")
        return "\n".join(lines) if lines else "无已知信息"


class ProfileLearningEngine:
    """画像学习引擎——从对话中自动学习。"""

    EXTRACT_PROMPT = """从以下对话中提取用户信息。只提取明确提及的。

对话:
&#123;conversation&#125;

输出JSON:
```json
&#123;&#123;
  "facts": [&#123;&#123;"key": "姓名", "value": "张三"&#125;&#125;],
  "preferences": [&#123;&#123;"key": "回答风格", "value": "简洁技术性"&#125;&#125;]
&#125;&#125;
```
无新信息返回空数组。"""

    def __init__(self, llm: BaseChatModel):
        self.llm = llm

    async def learn(self, profile: UserProfile, conversation: str) -> UserProfile:
        """从对话中学习用户信息。"""
        prompt = self.EXTRACT_PROMPT.format(conversation=conversation[:1000])
        response = await self.llm.ainvoke([HumanMessage(content=prompt)])

        match = re.search(r'\&#123;.*\&#125;', response.content, re.DOTALL)
        if match:
            data = json.loads(match.group())
            for fact in data.get("facts", []):
                if fact["key"] == "姓名":
                    profile.name = fact["value"]
            for pref in data.get("preferences", []):
                profile.add_preference(pref["key"], pref["value"])

        return profile


class PersonalizationInjector:
    """个性化注入器——将画像注入系统提示。"""

    @staticmethod
    def build_system_prompt(base_prompt: str, profile: UserProfile) -> str:
        """构建个性化系统提示。"""
        profile_text = profile.get_profile_text()
        if profile_text == "无已知信息":
            return base_prompt

        return f"""&#123;base_prompt&#125;

## 用户画像
&#123;profile_text&#125;

## 个性化要求
- 用用户的偏好语言回答
- 适配用户技能水平
- 记住用户已知信息，不重复问"""
```

---

## 三、最佳实践

| 实践 | 说明 | 优先级 |
|------|------|--------|
| 从对话自动学习 | 不需用户手动填写 | ★★★ |
| 画像注入系统提示 | 个性化回答 | ★★★ |
| 用户可查看和删除 | 隐私合规 | ★★★ |
| 画像有版本管理 | 可追溯 | ★★☆ |
| 偏好会过时 | 定期更新 | ★★☆ |

---

## 四、检查清单

| 检查项 | 状态 |
|--------|------|
| 有画像数据结构 | ☐ |
| 有学习引擎 | ☐ |
| 有注入器 | ☐ |
