# 增量更新图解

> 用图解理解变更检测和增量应用。

---

## 一、变更检测

```mermaid
graph TB
    SCAN["扫描文件"] --> HASH&#123;"hash变了?"&#125;
    HASH -->|没变| SKIP["跳过"]
    HASH -->|变了| TYPE&#123;"类型?"&#125;
    TYPE -->|新增| ADD["嵌入+入库"]
    TYPE -->|修改| DEL_ADD["删旧+重嵌入"]
    TYPE -->|删除| DEL["删向量"]

    style HASH fill:#FFF9C4
```

---

## 二、检查清单

| 检查项 | 状态 |
|--------|------|
| 有hash检测 | ☐ |
| 有增量应用 | ☐ |
