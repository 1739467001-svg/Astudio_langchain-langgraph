# 模型选型决策图解

> 用图解理解模型对比矩阵和场景化选型。

---

## 一、选型维度

```mermaid
graph TB
    ROOT["选型维度"] --> D1["效果"]
    ROOT --> D2["成本"]
    ROOT --> D3["延迟"]
    ROOT --> D4["语言"]
    ROOT --> D5["隐私"]
    ROOT --> D6["生态"]

    style ROOT fill:#1565C0,color:#fff
    style D1 fill:#C8E6C9
    style D2 fill:#FFCDD2
```

---

## 二、场景化选型

```mermaid
graph TB
    Q1["场景?"] -->|实时对话| Q2["可出境?"]
    Q2 -->|是| Q3["预算?"]
    Q3 -->|高| GPT["GPT-4o"]
    Q3 -->|低| MINI["GPT-4o-mini"]
    Q2 -->|否| QWEN["Qwen本地"]
    Q1 -->|批量| DS["DeepSeek-V3"]
    Q1 -->|代码| CLAUDE["Claude 3.5"]
    Q1 -->|多模态| GPT2["GPT-4o"]

    style GPT fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
    style MINI fill:#E3F2FD
    style QWEN fill:#FFF3E0
```

---

## 三、成本对比

```mermaid
graph TB
    GPT4["GPT-4o: $75/月"]
    MINI["GPT-4o-mini: $4.5/月"]
    CLAUDE["Claude 3.5: $90/月"]
    QWEN["Qwen本地: $0(仅服务器)"]
    DS["DeepSeek: $4.2/月"]

    style GPT4 fill:#FFCDD2
    style MINI fill:#C8E6C9
    style QWEN fill:#C8E6C9
```

---

## 四、混合策略

```mermaid
graph TB
    subgraph 混合 &#123;"多模型组合"&#125;
        M1["80%→GPT-4o-mini"]
        M2["复杂→GPT-4o"]
        M3["代码→Claude 3.5"]
        M4["批量→DeepSeek"]
        M5["敏感→Qwen本地"]
    end

    style 混合 fill:#E3F2FD
```

---

## 五、检查清单

| 检查项 | 状态 |
|--------|------|
| 有多维对比 | ☐ |
| 按场景选模型 | ☐ |
| 有多模型路由 | ☐ |
| 有成本对比 | ☐ |
