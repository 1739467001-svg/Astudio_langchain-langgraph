# LLM 应用架构模式图解

> 用图解理解 8 种架构模式的核心思想和选型决策。

---

## 一、8种模式总览

```mermaid
graph TB
    ROOT["架构模式"] --> P1["Request-Response"]
    ROOT --> P2["Streaming"]
    ROOT --> P3["Async Queue"]
    ROOT --> P4["CQRS"]
    ROOT --> P5["Event Sourcing"]
    ROOT --> P6["Saga"]
    ROOT --> P7["Pipeline"]
    ROOT --> P8["Supervisor"]

    style ROOT fill:#1565C0,color:#fff
    style P4 fill:#FFF9C4,stroke:#F9A825,stroke-width:3px
```

---

## 二、CQRS（读写分离）

```mermaid
graph TB
    subgraph 写 &#123;"写路径(离线)"&#125;
        W1["文档→ETL→嵌入→向量库"]
    end
    subgraph 读 &#123;"读路径(在线)"&#125;
        R1["查询→检索→生成"]
    end

    style 写 fill:#E3F2FD,stroke:#1565C0,stroke-width:3px
    style 读 fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
```

---

## 三、Async Queue

```mermaid
graph LR
    U["用户"] --> API["API返回task_id"]
    API --> QUEUE["任务队列"]
    QUEUE --> WORKER["Worker执行"]
    WORKER --> NOTIFY["通知用户"]

    style QUEUE fill:#FFF9C4
```

---

## 四、Event Sourcing

```mermaid
graph TB
    E1["事件1: 用户问X"] --> STORE["事件存储"]
    E2["事件2: Agent搜索Y"] --> STORE
    E3["事件3: Agent回答Z"] --> STORE
    STORE --> REPLAY["重放→重建状态"]

    style STORE fill:#FFF9C4
```

---

## 五、Saga

```mermaid
graph LR
    S1["搜索"] --> S2["分析"] --> S3["生成"] --> S4["发送"]
    S2 -.->|失败| C1["补偿: 清缓存"]
    S3 -.->|失败| C2["补偿: 删分析"]

    style S4 fill:#C8E6C9
    style C1 fill:#FFCDD2
```

---

## 六、选型决策

```mermaid
graph TB
    Q1["延迟<1s?"] -->|是| Q2["需流式?"]
    Q2 -->|是| STREAM["Streaming"]
    Q2 -->|否| RR["Request-Response"]
    Q1 -->|否| ASYNC["Async Queue"]
    Q3["多步操作?"] -->|是| SAGA["Saga"]
    Q4["RAG读写?"] -->|是| CQRS["CQRS"]

    style STREAM fill:#C8E6C9
    style CQRS fill:#FFF9C4
```

---

## 七、检查清单

| 检查项 | 状态 |
|--------|------|
| 理解8种模式 | ☐ |
| 知道CQRS在RAG的应用 | ☐ |
| 理解Event Sourcing | ☐ |
| 能根据场景选模式 | ☐ |
