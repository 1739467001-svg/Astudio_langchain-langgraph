# RAG 离线评估图解

> 用图解理解离线评估流程、基线对比和CI集成。

---

## 一、离线 vs 在线

```mermaid
graph TB
    subgraph 在线 &#123;"在线"&#125;
        O1["真实查询<br/>慢且贵"]
    end
    subgraph 离线 &#123;"离线"&#125;
        L1["预构建测试集<br/>快且省"]
    end

    style 在线 fill:#FFF3E0
    style 离线 fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
```

---

## 二、评估流程

```mermaid
graph LR
    LOAD["加载测试集"] --> RUN["批量执行"] --> EVAL["评估"] --> REPORT["报告"]

    style RUN fill:#FFF9C4
```

---

## 三、基线对比

```mermaid
graph TB
    BASELINE["基线 pass_rate=82%"] --> CURRENT["当前 pass_rate=85%"]
    CURRENT --> DELTA&#123;"Δ=+3%"&#125;
    DELTA -->|改进| PASS["✅ 通过"]
    DELTA -->|退化| BLOCK["❌ 阻止"]

    style PASS fill:#C8E6C9
    style BLOCK fill:#FFCDD2
```

---

## 四、检查清单

| 检查项 | 状态 |
|--------|------|
| 有离线评估器 | ☐ |
| 有基线对比 | ☐ |
| 有CI集成 | ☐ |
