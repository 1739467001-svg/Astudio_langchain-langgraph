# 上下文窗口管理图解

> 用图解理解 LangGraph 中 messages 字段管理和 Token 预算控制。

---

## 一、上下文问题

```mermaid
graph TB
    P1["messages无限增长"]
    P2["工具结果膨胀"]
    P3["多轮后Token超限"]

    style P1 fill:#FFCDD2
```

---

## 二、压缩流程

```mermaid
graph TB
    START["START"] --> COMPRESS["compress<br/>压缩历史+工具结果"]
    COMPRESS --> RESPOND["respond<br/>生成回答"]
    RESPOND --> END["END"]

    style COMPRESS fill:#FFF9C4,stroke:#F9A825,stroke-width:3px
```

---

## 三、RemoveMessage

```mermaid
graph LR
    OLD["旧消息"] --> REMOVE["RemoveMessage"]
    REMOVE --> SUMMARY["摘要替代"]
    SUMMARY --> KEEP["最近3轮原文"]

    style REMOVE fill:#FFF9C4
```

---

## 四、检查清单

| 检查项 | 状态 |
|--------|------|
| 有历史压缩节点 | ☐ |
| 有工具结果压缩 | ☐ |
| 有Token预算检查 | ☐ |
