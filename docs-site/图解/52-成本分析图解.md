# 成本分析图解

> 用图解理解 LLM 应用的成本构成和优化策略。

---

## 一、成本构成

```mermaid
graph TB
    subgraph 成本 &#123;"LLM应用总成本构成"&#125;
        C1["LLM API费用<br/>60-80% 🔴"]
        C2["向量数据库<br/>5-10% 🟡"]
        C3["服务器<br/>10-20% 🟡"]
        C4["其他<br/>5% 🟢"]
    end

    style C1 fill:'#FFCDD2'
    style C2 fill:'#FFE0B2'
    style C3 fill:'#FFE0B2'
    style C4 fill:'#C8E6C9'
```

## 二、模型成本对比

```mermaid
graph TB
    subgraph 月度成本 &#123;"1000次/天 月度成本对比"&#125;
        G4["GPT-4o<br/>~$50/月"]
        GM["GPT-4o-mini<br/>~$3/月"]
        CL["Claude 3.5<br/>~$60/月"]
        DS["DeepSeek<br/>~$2/月"]
        QW["通义千问<br/>~$8/月"]
        GLM["GLM-Flash<br/>$0/月"]
    end

    style GM fill:'#C8E6C9'
    style GLM fill:'#C8E6C9'
    style G4 fill:'#FFE0B2'
    style CL fill:'#FFCDD2'
```

## 三、缓存对成本的影响

```mermaid
graph LR
    subgraph 缓存影响 &#123;"缓存命中率对成本的影响"&#125;
        NO["无缓存<br/>100%调用API<br/>100%成本"]
        LOW["30%命中<br/>70%调用API<br/>节省30%"]
        HIGH["70%命中<br/>30%调用API<br/>节省70%"]
    end

    NO --> LOW --> HIGH

    style NO fill:'#FFCDD2'
    style HIGH fill:'#C8E6C9'
```

## 四、优化策略效果

```mermaid
graph TD
    subgraph 七策略 &#123;"成本优化七策略及效果"&#125;
        O1["换小模型<br/>节省90%+"]
        O2["启用缓存<br/>节省30-70%"]
        O3["截断历史<br/>节省40%输入"]
        O4["减小k值<br/>节省30%上下文"]
        O5["max_tokens<br/>限制输出"]
        O6["精简Prompt<br/>节省10-30%"]
        O7["本地模型<br/>节省100%API费"]
    end

    style O1 fill:'#C8E6C9'
    style O7 fill:'#C8E6C9'
```

## 五、ROI 示例

```mermaid
graph TB
    subgraph 客服ROI &#123;"客服机器人ROI示例"&#125;
        COST["月度成本: $27"]
        VALUE["月度价值: 自动处理60000次<br/>节省人工$1200"]
        ROI["ROI: 4340%<br/>净收益: $1173/月"]
    end

    COST --> ROI
    VALUE --> ROI

    style ROI fill:'#C8E6C9'
```
