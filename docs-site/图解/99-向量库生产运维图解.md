# 向量库生产运维图解

> 用图解理解 HNSW 参数调优、蓝绿索引重建、零停机迁移和性能监控。

---

## 一、运维全景

```mermaid
graph TB
    subgraph 运维 &#123;"向量库运维体系"&#125;
        O1["索引调优<br/>HNSW参数"]
        O2["数据管理<br/>重建/增量"]
        O3["迁移升级<br/>零停机"]
        O4["性能监控<br/>延迟/召回/内存"]
        O5["高可用<br/>副本/备份"]
    end

    style 运维 fill:#E3F2FD
```

---

## 二、HNSW原理

```mermaid
graph TB
    subgraph 分层 &#123;"HNSW分层搜索"&#125;
        L2["Layer 2: 少量节点<br/>超稀疏连接"]
        L1["Layer 1: 部分节点<br/>稀疏连接"]
        L0["Layer 0: 全部节点<br/>密集连接"]

        L2 -->|下降| L1
        L1 -->|下降| L0

        SEARCH["搜索: 从顶层入口<br/>→快速接近→逐层精确"]
    end

    style 分层 fill:#E3F2FD
    style SEARCH fill:#FFF9C4
```

---

## 三、两大核心参数

```mermaid
graph TB
    subgraph 参数 &#123;"M和ef参数"&#125;
        M["M=16<br/>每个节点连接数<br/>↑精度↑内存↑"]
        EF_C["ef_construction=200<br/>构建时搜索宽度<br/>↑精度↑构建慢"]
        EF_S["ef_search=64<br/>查询时搜索宽度<br/>↑召回↑查询慢"]
    end

    subgraph 权衡 &#123;"参数权衡"&#125;
        W1["M↑: 精度↑ 内存↑"]
        W2["ef_search↑: 召回↑ 延迟↑"]
        W3["需要在精度/内存/速度间平衡"]
    end

    style 参数 fill:#FFF3E0
    style 权衡 fill:#FFF9C4
```

---

## 四、参数调优实验

```mermaid
graph TB
    subgraph 实验 &#123;"参数搜索流程"&#125;
        C1["配置1: M=8<br/>最快, 召回低"]
        C2["配置2: M=16<br/>默认, 均衡"]
        C3["配置3: M=32<br/>精度高, 慢"]
        C4["配置4: M=48<br/>最高精度, 最慢"]
        C1 & C2 & C3 & C4 --> RESULT["选召回>95%<br/>且延迟最低"]
    end

    style RESULT fill:#C8E6C9
```

---

## 五、蓝绿索引重建

```mermaid
graph TB
    BLUE["Blue索引<br/>当前在线"] --> GREEN["Green索引<br/>后台构建"]
    GREEN --> VERIFY&#123;"验证通过？"&#125;
    VERIFY -->|是| SWITCH["切换流量"]
    VERIFY -->|否| RETRY["修复重建"]
    SWITCH --> DELETE["删除旧Blue"]
    DELETE --> DONE["完成"]

    style BLUE fill:#E3F2FD
    style GREEN fill:#FFF3E0
    style SWITCH fill:#C8E6C9
    style VERIFY fill:#FFF9C4
```

---

## 六、零停机迁移

```mermaid
graph LR
    subgraph 迁移 &#123;"零停机迁移流程"&#125;
        S1["源库"] --> DUAL["双写阶段<br/>同时写源和目标"]
        DUAL --> BACKFILL["回填历史数据"]
        BACKFILL --> VERIFY["验证对比"]
        VERIFY --> CUTOVER["流量切换"]
        CUTOVER --> CLEANUP["清理源库"]
    end

    style DUAL fill:#FFF9C4
    style VERIFY fill:#FFF3E0
    style CUTOVER fill:#C8E6C9
```

---

## 七、监控指标

```mermaid
graph TB
    subgraph 监控 &#123;"5大监控指标"&#125;
        M1["查询延迟<br/>P50/P95/P99<br/>目标P99<50ms"]
        M2["召回率<br/>定期抽检<br/>目标>95%"]
        M3["内存<br/>向量+索引<br/>目标<80%"]
        M4["QPS<br/>查询吞吐<br/>跟随业务"]
        M5["插入延迟<br/>批量写入<br/>目标<1s/batch"]
    end

    style 监控 fill:#E3F2FD
```

---

## 八、向量维度选择

```mermaid
graph TB
    subgraph 维度 &#123;"常见嵌入模型维度"&#125;
        D1["OpenAI 3-small<br/>1536维<br/>通用"]
        D2["OpenAI 3-large<br/>3072维<br/>高精度"]
        D3["BGE-large-zh<br/>1024维<br/>中文优化"]
        D4["Cohere v3<br/>1024维<br/>多语言"]
    end

    subgraph 权衡 &#123;"维度权衡"&#125;
        W1["维度↑: 精度↑ 存储↑ 查询慢↓"]
        W2["维度↓: 精度↓ 存储↓ 查询快↑"]
    end

    style 维度 fill:#E3F2FD
    style 权衡 fill:#FFF9C4
```

---

## 九、检查清单

| 检查项 | 状态 |
|--------|------|
| 理解M和ef参数 | ☐ |
| 做过参数调优 | ☐ |
| 有索引重建方案 | ☐ |
| 有零停机迁移流程 | ☐ |
| 有查询延迟监控 | ☐ |
| 有召回率检查 | ☐ |
