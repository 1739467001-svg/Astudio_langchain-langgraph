# LLM 推理优化图解

> 用图解理解 KV Cache、推测解码、量化和部署方案。

---

## 一、优化全景

```mermaid
graph TB
    ROOT["推理优化"] --> L1["模型: 量化/蒸馏"]
    ROOT --> L2["引擎: KV Cache/批处理"]
    ROOT --> L3["系统: GPU/显存"]
    ROOT --> L4["应用: 缓存/路由"]

    style ROOT fill:#1565C0,color:#fff
    style L2 fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
```

---

## 二、KV Cache

```mermaid
graph LR
    subgraph 无缓存 {"无KV Cache"}
        T1["每Token重算所有QKV"]
    end
    subgraph 有缓存 {"有KV Cache"}
        T2["复用缓存+只算新Token"]
    end

    style 无缓存 fill:#FFCDD2
    style 有缓存 fill:#C8E6C9
```

---

## 三、推测解码

```mermaid
graph TB
    D["草稿模型(快)"] -->|"猜N个Token"| DRAFT["草稿序列"]
    T["目标模型(准)"] -->|"并行验证"| VERIFY["验证"]
    VERIFY -->|"接受正确+拒绝错误"| ACCEPT["继续"]

    style D fill:#E3F2FD
    style T fill:#FFF9C4
```

---

## 四、量化方案

```mermaid
graph TB
    FP16["FP16: 100%显存"]
    INT8["INT8: 50%显存<br/>损失<1%"]
    INT4["INT4 AWQ: 25%显存<br/>损失2-5%"]

    style INT4 fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
```

---

## 五、部署方案

```mermaid
graph TB
    VLLM["vLLM<br/>生产推荐"]
    OLLAMA["Ollama<br/>开发简单"]
    SGLANG["SGLang<br/>极低延迟"]
    CPP["llama.cpp<br/>CPU部署"]

    style VLLM fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
```

---

## 六、检查清单

| 检查项 | 状态 |
|--------|------|
| 理解KV Cache | ☐ |
| 理解推测解码 | ☐ |
| 知道量化选择 | ☐ |
| 能选部署方案 | ☐ |
