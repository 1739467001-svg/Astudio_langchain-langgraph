# 配置即代码图解

> 用图解理解 YAML 配置、热更新和版本回滚。

---

## 一、配置架构

```mermaid
graph LR
    YAML["YAML配置<br/>Git管理"] --> VALIDATE["验证"]
    VALIDATE --> LOAD["加载"]
    LOAD --> APP["应用执行"]

    style YAML fill:#FFF9C4,stroke:#F9A825,stroke-width:3px
    style APP fill:#C8E6C9
```

---

## 二、配置内容

```mermaid
graph TB
    subgraph 配置 {"YAML配置内容"}
        C1["模型配置"]
        C2["Prompt版本"]
        C3["工具+权限"]
        C4["RAG参数"]
        C5["缓存配置"]
        C6["安全配置"]
        C7["监控配置"]
    end

    style 配置 fill:#E3F2FD
```

---

## 三、热更新

```mermaid
graph TB
    CHECK["检查文件变更"] --> CHANGED{"有变更?"}
    CHANGED -->|是| RELOAD["重新加载"] --> NOTIFY["通知回调"]
    CHANGED -->|否| WAIT["继续等待"]

    style CHANGED fill:#FFF9C4
    style RELOAD fill:#C8E6C9
```

---

## 四、版本回滚

```mermaid
graph LR
    V3["v1.3 当前"] -->|"回滚"| V2["v1.2 上一个"]
    V2 -->|"再回滚"| V1["v1.1 更早"]

    style V2 fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
```

---

## 五、检查清单

| 检查项 | 状态 |
|--------|------|
| 有YAML配置 | ☐ |
| 有配置验证 | ☐ |
| 有热更新 | ☐ |
| 有版本回滚 | ☐ |
