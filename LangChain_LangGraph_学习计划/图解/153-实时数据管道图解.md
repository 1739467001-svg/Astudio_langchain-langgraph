# 实时数据管道图解

> 用图解理解数据源接入、流处理和实时索引。

---

## 一、管道架构

```mermaid
graph LR
    SRC["数据源"] --> STREAM["事件队列"]
    STREAM --> PROC["流处理"]
    PROC --> INDEX["实时索引"]
    INDEX --> RAG["实时RAG"]

    style STREAM fill:#FFF9C4,stroke:#F9A825,stroke-width:3px
    style RAG fill:#C8E6C9
```

---

## 二、数据源

```mermaid
graph TB
    subgraph 源 {"4种数据源"}
        S1["Webhook推送"]
        S2["文件监听"]
        S3["数据库CDC"]
        S4["API轮询"]
    end

    style 源 fill:#E3F2FD
```

---

## 三、事件处理

```mermaid
graph TB
    EVENT["数据事件"] --> TYPE{"类型?"}
    TYPE -->|create| C["分块→嵌入→入库"]
    TYPE -->|update| U["删旧→重新处理"]
    TYPE -->|delete| D["删除向量"]

    style TYPE fill:#FFF9C4
```

---

## 四、检查清单

| 检查项 | 状态 |
|--------|------|
| 有事件队列 | ☐ |
| 有流处理器 | ☐ |
| 有增量更新 | ☐ |
| 有处理统计 | ☐ |
