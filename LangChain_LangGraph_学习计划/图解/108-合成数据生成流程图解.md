# 合成数据生成流程图解

> 用图解理解合成数据的5种应用场景、生成流程和质量控制。

---

## 一、5种应用场景

```mermaid
graph TB
    ROOT["合成数据应用"] --> A1["测试集生成"]
    ROOT --> A2["训练集扩充"]
    ROOT --> A3["Few-Shot示例"]
    ROOT --> A4["攻击载荷"]
    ROOT --> A5["对话模拟"]

    style ROOT fill:#1565C0,color:#fff
    style A1 fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
```

---

## 二、生成流程

```mermaid
graph TB
    S1["定义Schema"] --> S2["编写种子提示"]
    S2 --> S3["LLM批量生成"]
    S3 --> S4["去重+验证"]
    S4 --> S5["人工抽检10%"]
    S5 --> S6["入库"]

    style S3 fill:#FFF9C4,stroke:#F9A825,stroke-width:3px
    style S6 fill:#C8E6C9
```

---

## 三、质量控制

```mermaid
graph TB
    subgraph QC {"5步质量控制"}
        Q1["1.去重<br/>相同输入只留一个"]
        Q2["2.格式验证<br/>字段完整"]
        Q3["3.多样性检查<br/>不要太相似"]
        Q4["4.一致性检查<br/>输入输出匹配"]
        Q5["5.人工抽检<br/>10%审核"]
    end

    style QC fill:#E3F2FD
```

---

## 四、批量生成

```mermaid
graph LR
    TARGET["目标100条"] --> BATCH["分5批<br/>每批20条"]
    BATCH --> PARALLEL["并行生成"]
    PARALLEL --> DEDUPE["去重+验证"]
    DEDUPE → RESULT["最终数据集"]

    style PARALLEL fill:#FFF9C4
    style RESULT fill:#C8E6C9
```

---

## 五、多样性检查

```mermaid
graph TB
    INPUTS["输入列表"] --> SIM["计算两两Jaccard相似度"]
    SIM --> AVG["平均相似度"]
    AVG --> SCORE["多样性=1-avg_sim"]
    SCORE -->|"高>0.7"| GOOD["✅ 质量高"]
    SCORE -->|"中0.4-0.7"| MID["⚠️ 质量中"]
    SCORE -->|"低<0.4"| BAD["❌ 太同质"]

    style GOOD fill:#C8E6C9
    style MID fill:#FFF9C4
    style BAD fill:#FFCDD2
```

---

## 六、检查清单

| 检查项 | 状态 |
|--------|------|
| 能生成测试用例 | ☐ |
| 能生成对话数据 | ☐ |
| 有去重和多样性检查 | ☐ |
| 能批量生成 | ☐ |
| 有人工抽检 | ☐ |
