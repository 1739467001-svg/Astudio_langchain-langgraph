# LLM 应用多环境管理

> 开发在本地、测试在 staging、生产在 prod——三套环境的 Prompt、模型、知识库配置都不同。手动切换容易出错。这份指南讲透多环境管理的最佳实践。

---

## 一、多环境架构

```mermaid
graph TB
    subgraph 环境 {"三套环境"}
        DEV["开发环境<br/>本地<br/>GPT-4o-mini<br/>小知识库"]
        STAGING["测试环境<br/>staging<br/>GPT-4o<br/>完整知识库副本"]
        PROD["生产环境<br/>生产<br/>GPT-4o+路由<br/>完整知识库"]
    end

    DEV -->|"验证通过"| STAGING
    STAGING -->|"灰度验证"| PROD

    style DEV fill:#E3F2FD
    style STAGING fill:#FFF9C4
    style PROD fill:#C8E6C9,stroke:#2E7D32,stroke-width:3px
```

---

## 二、环境配置管理

```python
from dataclasses import dataclass, field
from enum import Enum
import os

class Environment(str, Enum):
    DEVELOPMENT = "development"
    STAGING = "staging"
    PRODUCTION = "production"

@dataclass
class EnvironmentConfig:
    """环境配置。"""
    env: Environment
    llm_model: str
    llm_fallback: str
    embedding_model: str
    vectorstore_url: str
    cache_enabled: bool
    cache_ttl: int
    max_tokens: int
    rate_limit_qps: int
    enable_tracing: bool
    enable_red_team: bool  # 红队测试
    api_keys: dict = field(default_factory=dict)

# 各环境配置
ENV_CONFIGS = {
    Environment.DEVELOPMENT: EnvironmentConfig(
        env=Environment.DEVELOPMENT,
        llm_model="gpt-4o-mini",
        llm_fallback="gpt-4o-mini",
        embedding_model="BAAI/bge-small-zh",
        vectorstore_url="http://localhost:19530",
        cache_enabled=False,
        cache_ttl=60,
        max_tokens=500,
        rate_limit_qps=10,
        enable_tracing=False,
        enable_red_team=False,
        api_keys={"openai": os.getenv("OPENAI_API_KEY_DEV", "")},
    ),
    Environment.STAGING: EnvironmentConfig(
        env=Environment.STAGING,
        llm_model="gpt-4o",
        llm_fallback="gpt-4o-mini",
        embedding_model="BAAI/bge-large-zh",
        vectorstore_url="http://staging-milvus:19530",
        cache_enabled=True,
        cache_ttl=300,
        max_tokens=1000,
        rate_limit_qps=50,
        enable_tracing=True,
        enable_red_team=True,
        api_keys={"openai": os.getenv("OPENAI_API_KEY_STAGING", "")},
    ),
    Environment.PRODUCTION: EnvironmentConfig(
        env=Environment.PRODUCTION,
        llm_model="gpt-4o",
        llm_fallback="gpt-4o-mini",
        embedding_model="BAAI/bge-large-zh",
        vectorstore_url="http://prod-milvus:19530",
        cache_enabled=True,
        cache_ttl=3600,
        max_tokens=2000,
        rate_limit_qps=100,
        enable_tracing=True,
        enable_red_team=False,
        api_keys={"openai": os.getenv("OPENAI_API_KEY_PROD", "")},
    ),
}

class EnvironmentManager:
    """环境管理器。"""

    @staticmethod
    def get_config() -> EnvironmentConfig:
        """获取当前环境配置。"""
        env_name = os.getenv("APP_ENV", "development")
        env = Environment(env_name)
        return ENV_CONFIGS.get(env, ENV_CONFIGS[Environment.DEVELOPMENT])

    @staticmethod
    def is_production() -> bool:
        return os.getenv("APP_ENV") == "production"

    @staticmethod
    def is_staging() -> bool:
        return os.getenv("APP_ENV") == "staging"
```

---

## 三、环境间数据同步

```mermaid
graph TB
    subgraph 同步 {"环境间数据同步"}
        DEV_KB["开发知识库<br/>小量测试数据"] --> PUSH1["推送到staging"]
        STAGING_KB["staging知识库<br/>完整副本"] --> PROMOTE["验证后提升"]
        PROMOTE --> PROD_KB["生产知识库<br/>正式数据"]
    end

    style PUSH1 fill:#E3F2FD
    style PROMOTE fill:#C8E6C9
```

```python
class EnvironmentSyncManager:
    """环境间同步管理器。"""

    @staticmethod
    def sync_knowledge_base(
        from_env: Environment,
        to_env: Environment,
        doc_ids: list[str] = None,
    ):
        """同步知识库。"""
        return {
            "from": from_env.value,
            "to": to_env.value,
            "docs_synced": len(doc_ids) if doc_ids else "all",
            "status": "synced",
        }

    @staticmethod
    def promote_config(from_env: Environment, to_env: Environment):
        """配置提升（staging→prod）。"""
        from_config = ENV_CONFIGS[from_env]
        to_config = ENV_CONFIGS[to_env]

        changes = []
        if from_config.llm_model != to_config.llm_model:
            changes.append(f"模型: {to_config.llm_model} → {from_config.llm_model}")
        if from_config.cache_ttl != to_config.cache_ttl:
            changes.append(f"缓存TTL: {to_config.cache_ttl} → {from_config.cache_ttl}")

        return {
            "changes": changes,
            "requires_approval": to_env == Environment.PRODUCTION,
        }
```

---

## 四、最佳实践

| 实践 | 说明 | 优先级 |
|------|------|--------|
| API Key分环境 | 不同环境不同Key | ★★★ |
| 开发用小模型 | 省成本 | ★★★ |
| Staging要追踪 | 测试可观测性 | ★★★ |
| 配置用环境变量 | 不硬编码 | ★★★ |
| Staging→Prod要审批 | 不能直接推 | ★★☆ |
| 知识库定期同步 | 保持一致 | ★★☆ |

---

## 五、检查清单

| 检查项 | 状态 |
|--------|------|
| 有三套环境配置 | ☐ |
| API Key分环境 | ☐ |
| 有环境间同步 | ☐ |
| 有配置提升审批 | ☐ |
