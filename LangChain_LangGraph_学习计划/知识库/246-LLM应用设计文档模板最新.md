# LLM 应用设计文档模板最新

> 知识库 70 仅 138 行、知识库 167 有完整模板。这篇提供精简版模板——一个页面内填完的设计文档。

---

## 一、精简设计模板

```markdown
# {项目名} 设计文档

## 基本信息
- 版本: v1.0
- 作者: {姓名}
- 日期: {日期}
- 状态: 草稿/评审中/已批准

## 1. 目标
{一句话描述目标}
- 成功标准: {可量化}

## 2. 架构
{架构图(Mermaid)}
- 核心组件: {列表}
- 技术栈: LangGraph+{其他}

## 3. 数据
- 数据源: {列表}
- 分块策略: {参数}
- 嵌入模型: {选择}
- 向量库: {选择}
- 数据安全: {脱敏/权限}

## 4. 模型
- 主模型: {GPT-4o}
- 备用模型: {mini}
- 降级方案: {链}

## 5. 安全
- [ ] 输入验证
- [ ] PII脱敏
- [ ] 工具权限
- [ ] 高危审批

## 6. 运维
- SLO: {可用性/延迟}
- 监控: {指标}
- 告警: {规则}

## 7. 风险
| 风险 | 影响 | 缓解 |
|------|------|------|
| {风险} | {影响} | {措施} |

## 8. 里程碑
| 阶段 | 日期 | 验收 |
|------|------|------|
| MVP | {日期} | {标准} |
```

---

## 二、评审检查

```python
class DesignDocReviewer:
    """设计文档评审器。"""

    MUST_HAVE = [
        "目标可量化",
        "有架构图",
        "有技术栈选择",
        "有数据安全方案",
        "有降级策略",
        "有SLO定义",
        "有风险缓解",
        "有里程碑",
    ]

    @classmethod
    def review(cls, doc_content: str) -> dict:
        """检查文档是否包含必要内容。"""
        missing = []
        for item in cls.MUST_HAVE:
            # 简化检查
            keywords = {
                "目标可量化": ["成功标准", "量化", "指标"],
                "有架构图": ["mermaid", "graph", "架构"],
                "有技术栈选择": ["LangGraph", "技术栈", "模型"],
                "有数据安全方案": ["脱敏", "权限", "安全"],
                "有降级策略": ["降级", "fallback", "备用"],
                "有SLO定义": ["SLO", "可用性", "延迟"],
                "有风险缓解": ["风险", "缓解", "措施"],
                "有里程碑": ["里程碑", "MVP", "阶段"],
            }
            found = any(kw in doc_content for kw in keywords.get(item, [item]))
            if not found:
                missing.append(item)

        return {
            "complete": len(missing) == 0,
            "missing": missing,
            "score": round(1 - len(missing) / len(cls.MUST_HAVE), 2),
        }
```

---

## 三、检查清单

| 检查项 | 状态 |
|--------|------|
| 有精简模板 | ☐ |
| 有评审检查 | ☐ |
