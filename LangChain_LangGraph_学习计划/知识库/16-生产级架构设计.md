# 生产级架构设计

> 从学习项目到生产系统，需要考虑可靠性、可扩展性、安全性和可维护性。本指南覆盖 LLM 应用的生产架构。

---

## 一、学习项目 vs 生产系统

```mermaid
graph TB
    subgraph 学习项目 ["学习项目"]
        L1["单文件脚本"]
        L2["API Key 在 .env"]
        L3["内存存储"]
        L4["无监控"]
        L5["无错误恢复"]
        L6["单用户"]
    end

    subgraph 生产系统 ["生产系统"]
        P1["API 服务 + 前端"]
        P2["密钥管理服务"]
        P3["数据库持久化"]
        P4["监控告警"]
        P5["重试 + 降级"]
        P6["多用户 + 鉴权"]
        P7["速率限制"]
        P8["日志审计"]
    end

    学习项目 -.->|"演进"| 生产系统

    style 学习项目 fill:#E3F2FD
    style 生产系统 fill:#C8E6C9
```

## 二、生产架构全景

```mermaid
graph TB
    subgraph 客户端
        WEB["Web前端"]
        APP["移动端"]
        API_C["第三方API调用"]
    end

    subgraph 接入层 ["接入层"]
        LB["负载均衡<br/>(Nginx/ALB)"]
        AUTH["鉴权中间件<br/>(JWT/API Key)"]
        RATE["速率限制<br/>(Redis计数器)"]
    end

    subgraph 应用层 ["应用层"]
        API["FastAPI 服务"]
        QUEUE["任务队列<br/>(Celery/RQ)"]
        WORKER["Worker进程<br/>(处理长任务)"]
    end

    subgraph LLM层 ["LLM 编排层"]
        CHAIN["LangChain Chains"]
        GRAPH["LangGraph 工作流"]
        AGENT["Agents"]
        CACHE["缓存层<br/>(Redis)"]
    end

    subgraph 数据层 ["数据层"]
        VDB[("向量数据库<br/>FAISS/Milvus")]
        DB[("关系数据库<br/>PostgreSQL")]
        REDIS[("Redis<br/>缓存+会话")]
        FILES["文件存储<br/>(S3/OSS)"]
    end

    subgraph 基础设施 ["基础设施"]
        LOG["日志收集<br/>(ELK/Loki)"]
        MON["监控告警<br/>(Prometheus)"]
        TRACE["追踪<br/>(LangSmith)"]
    end

    WEB & APP & API_C --> LB
    LB --> AUTH --> RATE --> API
    API --> CHAIN & GRAPH & AGENT
    API --> QUEUE --> WORKER
    WORKER --> CHAIN & GRAPH
    CHAIN & GRAPH & AGENT --> CACHE
    CHAIN & GRAPH & AGENT --> VDB
    CHAIN & GRAPH & AGENT --> DB
    API --> DB
    API --> REDIS
    API --> FILES

    CHAIN -.-> TRACE
    GRAPH -.-> TRACE
    API -.-> LOG
    WORKER -.-> LOG
    API -.-> MON

    style 接入层 fill:#FFCDD2,stroke:#C62828
    style 应用层 fill:#E3F2FD,stroke:#1565C0
    style LLM层 fill:#FFF3E0,stroke:#E65100
    style 数据层 fill:#F3E5F5,stroke:#6A1B9A
    style 基础设施 fill:#C8E6C9,stroke:#2E7D32
```

## 三、分层详解

### 3.1 接入层

```mermaid
graph LR
    subgraph 接入层职责
        A1["鉴权<br/>验证用户身份"]
        A2["速率限制<br/>防止滥用和超额"]
        A3["请求验证<br/>参数检查和清理"]
        A4["SSL/TLS<br/>加密传输"]
    end

    style 接入层职责 fill:#FFE0B2
```

```python
# FastAPI 接入层示例
from fastapi import FastAPI, Depends, HTTPException, Request
from slowapi import Limiter
from slowapi.util import get_remote_address

app = FastAPI()
limiter = Limiter(key_func=get_remote_address)

# 速率限制
@app.post("/api/chat")
@limiter.limit("10/minute")  # 每分钟最多10次
async def chat(request: Request, body: ChatRequest):
    # 鉴权检查
    user = authenticate(request.headers.get("Authorization"))
    if not user:
        raise HTTPException(status_code=401, detail="未授权")

    # 输入验证
    if len(body.question) > 2000:
        raise HTTPException(status_code=400, detail="输入过长")

    # 处理
    result = await process_chat(body.question, user.id)
    return {"answer": result}
```

### 3.2 应用层

```mermaid
graph TB
    subgraph 请求处理模式
        SYNC["同步请求<br/>直接返回<br/>(<3秒的简单调用)"]
        ASYNC_TASK["异步任务<br/>提交队列→返回任务ID<br/>轮询结果<br/>(>3秒的复杂任务)"]
        STREAM["流式响应<br/>SSE/WebSocket<br/>逐字返回<br/>(用户体验好)"]
    end

    SYNC --> S1["FastAPI 端点"]
    ASYNC_TASK --> S2["Celery + Redis"]
    STREAM --> S3["StreamingResponse"]

    style SYNC fill:#C8E6C9
    style ASYNC_TASK fill:#FFE0B2
    style STREAM fill:#F3E5F5
```

```python
# 三种处理模式的代码

# 模式1：同步（简单问答）
@app.post("/api/ask")
@limiter.limit("30/minute")
async def ask(request: Request, body: QuestionRequest):
    user = authenticate(request)
    result = await chain.ainvoke({"input": body.question})
    return {"answer": result}

# 模式2：异步任务（复杂工作流）
from celery import Celery

celery_app = Celery("tasks", broker="redis://localhost:6379")

@celery_app.task
def process_complex_task(question: str, user_id: str):
    """长时间运行的LLM工作流"""
    result = langgraph_app.invoke({"input": question})
    # 保存结果到数据库
    save_result(user_id, result)
    return result

@app.post("/api/analyze")
async def analyze(body: AnalysisRequest):
    user = authenticate(request)
    task = process_complex_task.delay(body.question, user.id)
    return {"task_id": task.id, "status": "processing"}

@app.get("/api/task/{task_id}")
async def get_task_status(task_id: str):
    task = process_complex_task.AsyncResult(task_id)
    if task.ready():
        return {"status": "done", "result": task.result}
    return {"status": "processing"}

# 模式3：流式响应
from fastapi.responses import StreamingResponse

@app.post("/api/chat/stream")
@limiter.limit("10/minute")
async def chat_stream(request: Request, body: QuestionRequest):
    user = authenticate(request)
    async def generate():
        async for chunk in chain.astream({"input": body.question}):
            yield f"data: {chunk}\n\n"
    return StreamingResponse(generate(), media_type="text/event-stream")
```

### 3.3 数据层设计

```mermaid
graph TB
    subgraph 数据存储分工
        PG["PostgreSQL<br/>用户数据、对话历史<br/>结构化数据"]
        VDB[("向量数据库<br/>文档嵌入、知识库")]
        REDIS[("Redis<br/>缓存、会话、速率计数")]
        S3["对象存储<br/>上传的文件、图片"]
    end

    PG --> P1["users表: 用户信息"]
    PG --> P2["conversations表: 对话历史"]
    PG --> P3["usage表: 用量统计"]
    VDB --> V1["document_chunks表: 文档块+向量"]
    REDIS --> R1["session:{user_id} → 会话状态"]
    REDIS --> R2["rate:{user_id} → 请求计数"]
    REDIS --> R3["cache:{hash} → LLM响应缓存"]

    style PG fill:#E3F2FD
    style VDB fill:#F3E5F5
    style REDIS fill:#FFCDD2
    style S3 fill:#C8E6C9
```

### 3.4 可靠性设计

```mermaid
graph TB
    subgraph 可靠性策略
        R1["重试机制<br/>max_retries=3<br/>指数退避"]
        R2["超时控制<br/>LLM调用30秒超时<br/>总请求60秒超时"]
        R3["降级策略<br/>主模型不可用时<br/>切换备用模型"]
        R4["熔断器<br/>连续失败时<br/>暂时停止请求"]
        R5["队列缓冲<br/>高并发时排队<br/>避免压垮LLM API"]
    end

    style R1 fill:#E3F2FD
    style R2 fill:#FFF9C4
    style R3 fill:#FFE0B2
    style R4 fill:#FFCDD2
    style R5 fill:#F3E5F5
```

```python
# 降级策略示例
from langchain_openai import ChatOpenAI
from langchain_anthropic import ChatAnthropic
from langchain_ollama import ChatOllama

class ResilientLLM:
    """带降级能力的LLM调用器"""
    def __init__(self):
        self.primary = ChatOpenAI(model="gpt-4o-mini", max_retries=2, timeout=30)
        self.fallback = ChatAnthropic(model="claude-3-5-sonnet-20241022", max_retries=2)
        self.last_resort = ChatOllama(model="llama3")  # 本地模型兜底

    async def invoke(self, messages):
        """依次尝试主模型→备用→兜底"""
        for llm, name in [(self.primary, "OpenAI"), (self.fallback, "Claude"), (self.last_resort, "Ollama")]:
            try:
                return await llm.ainvoke(messages)
            except Exception as e:
                print(f"⚠️ {name} 调用失败: {e}，切换备用模型...")
                continue
        raise Exception("所有模型均不可用")
```

## 四、部署架构

```mermaid
graph TB
    subgraph Docker部署 ["Docker Compose 部署"]
        direction TB
        D_NGINX["nginx<br/>反向代理+SSL"]
        D_API["api 容器 (FastAPI)"]
        D_WORKER["worker 容器 (Celery)"]
        D_REDIS["redis 容器"]
        D_DB["postgres 容器"]

        D_NGINX --> D_API
        D_API --> D_REDIS
        D_API --> D_DB
        D_API --> D_WORKER
        D_WORKER --> D_REDIS
        D_WORKER --> D_DB
    end

    style Docker部署 fill:#E3F2FD
```

### docker-compose.yml

```yaml
version: "3.8"
services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - api

  api:
    build: .
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    env_file: .env
    depends_on:
      - db
      - redis

  worker:
    build: .
    command: celery -A tasks worker --loglevel=info
    env_file: .env
    depends_on:
      - redis

  db:
    image: postgres:16
    environment:
      POSTGRES_DB: llm_app
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    volumes:
      - redisdata:/data

volumes:
  pgdata:
  redisdata:
```

## 五、监控告警

```mermaid
graph TB
    subgraph 监控指标
        M1["业务指标<br/>QPS、错误率、延迟P95"]
        M2["LLM指标<br/>Token用量、成本、模型延迟"]
        M3["基础设施<br/>CPU/内存/磁盘/网络"]
        M4["用户体验<br/>首字延迟、完成率"]
    end

    subgraph 告警规则
        A1["错误率 > 5% → 立即告警"]
        A2["延迟P95 > 10秒 → 告警"]
        A3["日成本 > 预算80% → 告警"]
        A4["服务不可用 → 立即告警"]
    end

    style M1 fill:#E3F2FD
    style M2 fill:#FFE0B2
    style M3 fill:#C8E6C9
    style M4 fill:#F3E5F5
```

## 六、从学习到生产的演进路线

```mermaid
graph LR
    subgraph 阶段1 ["阶段1: 本地脚本"]
        S1["单文件Python脚本"]
        S1 --> S2
    end

    subgraph 阶段2 ["阶段2: API服务"]
        S2["FastAPI封装<br/>+ 基础鉴权"]
        S2 --> S3
    end

    subgraph 阶段3 ["阶段3: 生产就绪"]
        S3["+ 数据库持久化<br/>+ 速率限制<br/>+ 缓存<br/>+ LangSmith追踪"]
        S3 --> S4
    end

    subgraph 阶段4 ["阶段4: 完整生产"]
        S4["+ Docker部署<br/>+ 监控告警<br/>+ 降级熔断<br/>+ 任务队列"]
    end

    style 阶段1 fill:#C8E6C9
    style 阶段2 fill:#E3F2FD
    style 阶段3 fill:#FFE0B2
    style 阶段4 fill:#F3E5F5
```

| 阶段 | 核心任务 | 复杂度 |
|------|----------|--------|
| 1 | 跑通核心功能 | ★☆☆ |
| 2 | API 化 + 基础鉴权 | ★★☆ |
| 3 | 持久化 + 缓存 + 监控 | ★★★ |
| 4 | 容器化 + 高可用 + 告警 | ★★★★ |

不要试图一步到位——按阶段逐步演进，每步都验证可用后再进入下一步。
