# 速查卡最新

> 学习评估 03 有 239 行。这篇基于 670 篇文档更新——核心 API、常用模式和常见问题速查。

---

## 一、核心 API 速查

### 创建 Agent

```python
# 最简Agent
from langgraph.prebuilt import create_react_agent
agent = create_react_agent(llm, tools, checkpointer=MemorySaver())

# 带记忆+提示
agent = create_react_agent(llm, tools, checkpointer=MemorySaver(), store=store, prompt="你是助手")
```

### 构建图

```python
from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import add_messages
from typing import TypedDict, Annotated

class State(TypedDict):
    messages: Annotated[list, add_messages]

graph = StateGraph(State)
graph.add_node("node", handler)
graph.add_edge(START, "node")
graph.add_edge("node", END)
graph.add_conditional_edges("from", route_func, {"a": "a_node", "b": "b_node"})
app = graph.compile(checkpointer=MemorySaver())
```

### 流式

```python
# streaming=True必须
llm = ChatOpenAI(model="gpt-4o", streaming=True)

# 事件级流式
async for event in agent.astream_events(input, version="v2"):
    if event["event"] == "on_chat_model_stream":
        print(event["data"]["chunk"].content, end="")
```

### 人机交互

```python
from langgraph.types import interrupt, Command

# 中断
approval = interrupt({"question": "批准?"})

# 恢复
agent.invoke(Command(resume={"approved": True}), config)
```

### 状态管理

```python
# 查看状态
state = app.get_state(config)

# 修改状态
app.update_state(config, values={"key": "new_value"})

# 清理消息
from langgraph.graph.message import RemoveMessage
app.update_state(config, values={"messages": [RemoveMessage(id=msg.id)]})
```

---

## 二、常用模式速查

| 模式 | 代码 |
|------|------|
| LCEL管道 | `prompt | llm | parser` |
| 降级链 | `llm.with_fallbacks([backup])` |
| 重试 | `llm.with_retry(stop_after_attempt=3)` |
| 结构化输出 | `llm.with_structured_output(Model)` |
| 绑定工具 | `llm.bind_tools([tool1, tool2])` |
| 并行 | `graph.add_edge(START, "a"); graph.add_edge(START, "b")` |
| 条件路由 | `graph.add_conditional_edges("src", func, mapping)` |

---

## 三、常见问题速查

| 问题 | 解决 |
|------|------|
| Agent死循环 | 设recursion_limit |
| 流式无输出 | streaming=True |
| interrupt无法恢复 | 配checkpointer |
| 状态混淆 | 用唯一thread_id |
| Token超限 | RemoveMessage压缩 |
| JSON解析失败 | 提取片段+LLM修正 |

---

## 四、检查清单

| 检查项 | 状态 |
|--------|------|
| 有API速查 | ☐ |
| 有模式速查 | ☐ |
| 有问题速查 | ☐ |
